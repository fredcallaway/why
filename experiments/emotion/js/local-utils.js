var exp_sizing = function(){
    exp.width=800;
    exp.height=620;
    $('.slide').css('width', exp.width);
    $('.slide').css('height', exp.height);
    $('.exp').css('height', exp.height+2);
    $('.slide_wrapper').css('width', exp.width);
    $('.slide_wrapper').css('height', exp.height);
}
